#include "sort.h"


//void* q_sort(void* arr_of_ptrs_on_strs, size_t (tp -> quantity_strs), size_t size, int(*compare)(void* a, void* b));   

void bubble_sort(text_params* tp)
{
    char* symb_1 = NULL;
    char* symb_2 = NULL;

    for(size_t i = 0; i < tp -> quantity_strs; i++)
    {
        while(char* symb )
        {
            if(tp -> arr_of_ptrs_on_strs)
            {

            }
        }
    }
}